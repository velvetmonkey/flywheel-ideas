# SEC Company Ledgers

This directory is reserved for Markdown-only SEC ledger snapshots.

The first target corpus is `sec-10y-100-company/`, generated by:

```bash
npm run build -w @velvetmonkey/flywheel-ideas-core
node scripts/sec-sector-lifecycle-bundle.mjs
```

The local working ledger stays under `/home/ben/sec-dogfood/company-ledgers/`. Only generated Markdown from the vault is exported here for commit.
